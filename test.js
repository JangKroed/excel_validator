/**
 * @typedef {Object} CheckObj
 * @property {string | number} [key:string] how the person is called
 */

/**
 * @typeof Config
 * @type {object}
 * @property {'none' | 'id' | 'select' | 'range' | 'db' | 'regex'} type
 * @property {boolean} required
 * @property {boolean} unique
 * @property {regex} regex
 * @property {CheckObj} checkObj
 */
